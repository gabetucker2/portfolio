<?PHP

    //make a random gradient
    /*
    stopPoints = new Array();
    stopPoints.push(0);
    for (var p = 0; p < 9; p++) {
        stopPoints.push(Math.random());
    }
    stopPoints.push(1);//ranges from 0 to 11
    stopPoints.sort(function compare(a, b) { return a - b; });
    var isOther = false;
    for (var p = 0; p < 10; p++) {//0 to 10, leaves room for + 1
        var thisColor = "#000000";
        if (isOther) { thisColor = "#FFFFFF"; }
        secondGradString += thisColor + " " + (stopPoints[p]*100) + "% " + (stopPoints[p + 1]*100) + "%";
        if (p != 10 - 1) { secondGradString += ", "; }
        isOther = !isOther;
    }
    */

?>